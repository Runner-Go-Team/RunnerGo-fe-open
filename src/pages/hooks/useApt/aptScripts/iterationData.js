// 测试数据变量
export const iterationData = {
    has(variableName) {
        // console.log('iterationData has ', variableName);
    },
    get(variableName) {
        // console.log('iterationData get ', variableName);
    },
    set(variableName, variableValue) {
        // console.log('iterationData set ', variableName, variableValue);
    },
    replaceIn(variableName, variableValue) {
        // console.log('iterationData set ', variableName, variableValue);
    },
    toObject() {
        // console.log('variables toObject ', {});
    },
};

export default iterationData;
