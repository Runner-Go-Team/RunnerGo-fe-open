// import { User } from '@indexedDB/user';
import { get, isNull, isUndefined } from 'lodash';

export const setWorkspaceCurrent = async (uuid, jsonPath, value) => {
  // const count = await User.update(uuid, {
  //   [`workspace.${jsonPath}`]: value,
  // });
};

export const getWorkspaceCurrent = async (uuid, jsonPath) => {
  // if (isNull(uuid)) {
  //   return null;
  // }
  // const userConfig = await User.get(uuid);
  // if (isUndefined(userConfig)) {
  //   return null;
  // }
  // return get(userConfig, `workspace.${jsonPath}`, null);
  return {};
};
