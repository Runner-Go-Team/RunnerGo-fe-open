// import { Envs } from '@indexedDB/project';
// import { IEnv } from '@models/project/env';

// 获取本地全部环境信息
export const getLocalEnvsDatas = async (project_id) => {
    // const localEnvList = await Envs.where('project_id').equals(project_id).toArray();

    // const envData = {};
    // localEnvList.forEach((item) => {
    //     envData[`${item.env_id}`] = item;
    // });

    // return envData;
};
