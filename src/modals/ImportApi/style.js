import { css } from '@emotion/css';

export const ImportApiModal = css`
  .apipost-modal-container {
    height: 75%;
    width: 80%;
    max-width: 800px;
    min-width: 300px;
  }
  .import-title {
    font-size: var(--size-24px);
    margin: 4px 0 8px 0;
    color: var(--content-color-secondary);
  }
`;
